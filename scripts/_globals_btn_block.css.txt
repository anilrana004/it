/* ====== Button Base (retro offset shadow via retro-buttons.css) ====== */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-1);
  font-family: var(--font-sans);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  text-decoration: none;
  white-space: nowrap;
}
.btn:focus-visible { outline: 2px solid var(--ih-primary); outline-offset: 3px; }
.btn:disabled { opacity: 0.5; cursor: not-allowed; pointer-events: none; }

.btn-primary {
  background: var(--ih-primary);
  color: #ffffff;
  border-radius: var(--radius-button);
  padding: 8px 16px;
}
.btn-primary:hover:not(:disabled) { background: var(--ih-primary-hover); }

.btn-pill {
  background: var(--ih-secondary);
  color: #ffffff;
  border-radius: var(--radius-pill);
  padding: 12px 20px;
  --it-retro-ink: #0f3460;
  border: 2px solid var(--it-retro-ink);
  box-shadow: 3px 3px 0 0 var(--it-retro-ink);
  transition: transform 0.14s ease, box-shadow 0.14s ease, background-color 0.14s ease;
}
.btn-pill:hover:not(:disabled) {
  background: var(--ih-secondary-hover);
  transform: translate(2px, 2px);
  box-shadow: 1px 1px 0 0 var(--it-retro-ink);
}
.btn-pill:active:not(:disabled) {
  transform: translate(3px, 3px);
  box-shadow: none;
}

.btn-filled-2 {
  background: var(--ih-secondary);
  color: #ffffff;
  border-radius: 34px;
  padding: 12px 26px;
  font-size: 16px;
  font-weight: 700;
}
.btn-filled-2:hover:not(:disabled) { background: var(--ih-secondary-hover); }

.btn-ghost {
  background: #fff;
  color: var(--ih-border);
  border-radius: var(--radius-pill);
  padding: 8px 24px;
  font-size: 14px;
  font-weight: 700;
  border: 2px solid var(--ih-border);
  box-shadow: 3px 3px 0 0 var(--ih-border);
  transition: transform 0.14s ease, box-shadow 0.14s ease, background-color 0.14s ease;
}
.btn-ghost:hover:not(:disabled) {
  background: rgba(0, 0, 0, 0.04);
  transform: translate(2px, 2px);
  box-shadow: 1px 1px 0 0 var(--ih-border);
}
.btn-ghost:active:not(:disabled) {
  transform: translate(3px, 3px);
  box-shadow: none;
}

.btn-ghost-dark {
  background: transparent;
  color: var(--ih-text);
  border-radius: var(--radius-pill);
  padding: 8px 24px;
  font-size: 14px;
  font-weight: 600;
}
.btn-ghost-dark:hover:not(:disabled) { background: rgba(0, 0, 0, 0.05); }

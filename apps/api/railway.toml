[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build -w @indiantreks/api"

[deploy]
startCommand = "npm run start -w @indiantreks/api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

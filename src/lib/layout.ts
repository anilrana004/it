/** Shared chrome measurements (px) — keep Header + detail sticky nav in sync */
export const DESK_TOP_H = 28;
export const DESK_MAIN_H = 52;
export const DESK_HEADER_H = DESK_TOP_H + DESK_MAIN_H;
/** Mobile fixed wash bar (h-14) — excludes safe-area inset */
export const MOBILE_HEADER_H = 56;

/** Class toggled on <html> while site chrome is scrolled away (Roopkund flow). */
export const CHROME_HIDDEN_CLASS = 'it-chrome-hidden';
